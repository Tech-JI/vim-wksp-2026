# vim-wksp-2026

This repository is a consumer of `zzjc1234/nvim-portable`.

- `nvim/` contains the Neovim config source that gets bundled
- `.github/nvim/bootstrap.sh` bootstraps repo-specific runtime setup
- `.github/nvim/smoke.sh` and `.github/nvim/verify.sh` hold repo-specific checks
